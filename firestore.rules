rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // User profiles — readable by any signed-in user (needed for username search)
    match /users/{userId} {
      allow read: if request.auth != null;
      allow create, update: if request.auth != null && request.auth.uid == userId;
    }

    // Per-user chat list — own doc on create/read; any signed-in user can update
    // (AddUser writes to both users' userChats when starting a new chat)
    match /userChats/{userId} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow create: if request.auth != null && request.auth.uid == userId;
      allow update: if request.auth != null;
    }

    // Chat message threads
    match /chats/{chatId} {
      allow read, write: if request.auth != null;
    }
  }
}
