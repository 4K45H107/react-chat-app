rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Avatar uploads (see src/lib/upload.js — images/{filename})
    match /images/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
